apiVersion: apps/v1
kind: Deployment
metadata:
  name: step3
spec:
  replicas: 1
  selector:
    matchLabels: { app: step3 }
  template:
    metadata:
      labels: { app: step3 }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
    spec:
      containers:
      - name: step3
        image: saga/step3:dev
        imagePullPolicy: IfNotPresent
        ports: [{containerPort: 8080, name: metrics}]
        env:
        - name: KAFKA_BROKERS
          value: "kafka:9092"
        - name: GROUP_ID
          value: "svc3-group"
        - name: TOPIC_IN
          value: "saga.step2.completed"
        - name: TOPIC_OUT
          value: "saga.step3.completed"
        - name: DLQ_TOPIC
          value: "saga.dlq"
        - name: STEP
          value: "3"
        - name: JAEGER_COLLECTOR
          value: "http://jaeger-collector:14268/api/traces"
        readinessProbe:
          httpGet: { path: /metrics, port: 8080 }
          initialDelaySeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: step3
  labels:
    app: step3
    saga-metrics: "true"
spec:
  selector: { app: step3 }
  ports:
  - name: metrics
    port: 8080
    targetPort: 8080
